schema:
  http://localhost:8008/v1/graphql:
    headers:
      x-hasura-admin-secret: arthurquentinnolannetiennerobin

documents: 'src/**/*.graphql'
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      skipTypename: false
      withHooks: false
      withHOC: false
      withComponent: false
      scalars:
        uuid: string
        timestamptz: string
        jsonb: any
hooks:
  afterAllFileWrite:
    - prettier --write
